# 隐私说明

[English](PRIVACY.en.md)

最近更新：2026-08-29

ntscreenshot 是本机桌面应用。项目不运营统一账号，也不采集遥测。

## 本机存储

按开启的功能，程序可能写入：

- 偏好设置和集成密钥：Qt 应用数据目录下的 `config/base.ini`
- 诊断日志：同一应用数据目录的 `logs`
- 崩溃转储：工作目录中的 `ntscreenshot_*.dmp`（仅崩溃后）
- 剪贴板历史：Windows 上为 `%LOCALAPPDATA%/ClipboardLite/history.db`
- 本地搜索索引和网站图标缓存：Qt 应用数据目录
- 你主动选择保存位置的截图或录像

剪贴板历史和搜索索引可能包含个人敏感内容。分享配置目录或日志前请先清理，并保护好操作系统账户。

## 网络功能

截图、贴图、标注、剪贴板和本地搜索不依赖 ntscreenshot 服务器。只有你在设置中启用并实际调用时，才会向**你填写的第三方**发送数据，例如：

- LLM / 助手请求（可能含输入文本和你选中的图片）
- 网页搜索
- OCR 上传
- URL 抓取
- GitHub 图床

这些请求受对应服务商条款约束。发送前请确认目的地，避免上传机密内容。

若开启助手及其工具，模型可能按当前系统用户权限请求读文件、访问 URL、运行 Python / 命令行。请只配置可信服务商，核对工具调用，不要用管理员账户日常使用。

## 密钥

设置里填写的 API Key / Token 目前以明文写入本机 INI，没有额外的应用层加密。它们不会被打进发布包或提交到仓库。请使用可撤销、权限最小的密钥；若配置或日志可能泄露，请立即轮换。

## 停止与删除

在设置中关闭对应集成即可停止后续请求。退出程序后，可手动删除上述配置、索引、剪贴板数据库、日志和转储。删除不可恢复，请先备份需要留下的内容。

## 变更

对隐私有实质影响的改动会写进本文件和 [更新日志](CHANGELOG.md)。
